<!-- STATIC_PREFIX_START -->
# Percipience Bootstrapping Metaprompt

This metaprompt directs the autonomous initialization of fresh codebases into standardized Quad-Space structures.

## Objectives
1. Read initial project configuration and governing plans (`.nb/plan/` and `.nb/play/`).
2. Scaffold directory partitions: `context/`, `agentic/`, `workplace/`, and `user/`.
3. Initialize the Master Context Ledger (`context/ledger/context_ledger.yaml`) with Block 0 (GENESIS).
4. Populate standardized MVS ingestion templates in `user/inputs/templates/`.
5. Scaffold initial verification scripts, environment configs, and the interactive dashboard.
6. Verify cryptographic continuity and emit initial Context Maturity Scorecard ($\ge 0.85$).
<!-- STATIC_PREFIX_END -->

<!-- DYNAMIC_PAYLOAD_START -->
## Dynamic Request Context
- Target Module: ${TARGET_MODULE}
- Ingestion Payload: ${INGESTION_PAYLOAD}
- Dynamic Timestamp: ${EXECUTION_TIMESTAMP}
<!-- DYNAMIC_PAYLOAD_END -->
